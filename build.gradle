plugins {
    id 'maven-publish'
}

group = 'com.github.dewa1993'
version = '1.0.0'

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.github.dewa1993'
            artifactId = 'distribution-test'
            version = '1.0.0'
            
            artifact("libs/my-lib.aar")
        }
    }
}